
/**
 * The subclass of Rectangle
 * The Square class is used to represent the shape in a square pattern
 * @author natalie
 *
 */
public class Square extends Rectangle{
	public Square(int size){
		//subclass of rectangle but height = width
		super(size, size);
	}
}
